/**
 * Below are the colors that are used in the app.
 * DEPRECATED: Use definitions in '@/theme' or useTheme hook instead.
 */

import { LightTheme } from "@/theme/light";
import { DarkTheme } from "@/theme/dark";

export const Colors = {
  light: LightTheme,
  dark: DarkTheme,
};
